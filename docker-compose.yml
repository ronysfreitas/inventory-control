name: minasbrasil

services:
  postgres:
    image: postgres:16-alpine
    container_name: minasbrasil
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-minasbrasil}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - minasbrasil-postgres-data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  minasbrasil-postgres-data:
    name: minasbrasil-postgres-data
